-- 6. List the number of users each user follows.
SELECT (U.FIRST_NAME || ' ' || U.LAST_NAME) AS FULL_NAME, COUNT(F.USER_ID) AS NUM_FOLLOWED
FROM FOLLOWS F
LEFT JOIN
USERS U
ON F.USER_ID=U.USER_ID
GROUP BY FULL_NAME;
