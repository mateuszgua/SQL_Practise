-- 5. In 2016, what fraction of days did it rain?
SELECT 
	SUM(CASE WHEN DID_RAIN='TRUE' AND EXTRACT(YEAR FROM DATE_WEATHER ) = 2016
		THEN 1
		ELSE 0 END)::FLOAT/COUNT(*)
		AS RAIN_DAYS
FROM SEATTLE_WEATHER
WHERE EXTRACT(YEAR FROM DATE_WEATHER ) = 2016;
