-- 12. Give the average inches of rain that fell per day for each month, where the average is taken over 2000 - 2010 (inclusive).
SELECT EXTRACT(MONTH FROM DATE_WEATHER) AS MONTH, AVG(INCHES_RAIN) AS AVG_DAILY_RAIN
FROM SEATTLE_WEATHER
WHERE (EXTRACT(YEAR FROM DATE_WEATHER)>=2000) AND (EXTRACT(YEAR FROM DATE_WEATHER)<=2010)
GROUP BY 1
ORDER BY 1;