-- 10. Repeat the last question 9, but round the temperatures to 3 decimal places.
SELECT EXTRACT(YEAR FROM DATE_WEATHER) AS YEAR, ROUND(AVG(TEMP_MIN), 3) AS AVG_TEMP
FROM SEATTLE_WEATHER
WHERE EXTRACT(MONTH FROM DATE_WEATHER)=12
GROUP BY 1
ORDER BY 2
LIMIT 10;
